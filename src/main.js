import './styles.css';

const app = document.getElementById('app');

app.innerHTML = `
  <main class="viewport-center">
    <section class="status-card">
      <h1 class="status-title">App is running</h1>
      <p class="status-subtitle">Vite dev server on port 5173</p>
    </section>
  </main>
`;
